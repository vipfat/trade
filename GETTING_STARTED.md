# 🚀 БЫСТРЫЙ СТАРТ - ТОРГОВЫЙ БОТ НА AI

**Это файл для людей которые хотят просто запустить бота БЕЗ ЧИТАНИЯ ОГРОМНОЙ ДОКУМЕНТАЦИИ.**

Время на прочтение: **2 минуты**  
Время на установку: **5 минут**

---

## 1️⃣ Скопируйте код

Возьмите всю папку `/workspaces/trade` на вашу машину.

---

## 2️⃣ Установите зависимости

```bash
cd trade
pip install -r requirements.txt
```

**На Windows в PowerShell - нужно может быть:**
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
python -m pip install --upgrade pip
pip install -r requirements.txt
```

---

## 3️⃣ СРАЗУ ЗАПУСТИТЕ ОДНО ИЗ ЭТОГО

### ✅ ВАРИАНТ A - Самый простой (РЕКОМЕНДУЕТСЯ)
```bash
python quickstart.py
```
Выберите что вам нужно из меню. Всё просто.

---

### ✅ ВАРИАНТ B - Веб-интерфейс + Бот

**Откройте ДВА терминала:**

**Терминал 1:**
```bash
python run.py
```

**Терминал 2:**
```bash
python main.py --pairs 20 --testnet
```

Потом откройте браузер:
```
http://localhost:5000
Логин: admin
Пароль: admin123
```

Вы увидите красивый дашборд с логами в реальном времени! ✨

---

### ✅ ВАРИАНТ C - Просто демонстрация
```bash
python demo_mode.py
```
Показывает все возможности без запуска торговли.

---

## 📋 ЧТО ОЗНАЧАЮТ ЛОГИ

Смотрите в дашборде или в терминале:

```
[INFO] Загрузка данных BTCUSDT       ← Бот анализирует
[INFO] LSTM прогноз: 42500 (78%)     ← Нейросеть предсказывает
[INFO] Открыл позицию: 0.01 BTC      ← ТОРГОВЛЯ НАЧАЛАСЬ!
[INFO] Закрыл позицию: +$127 (3%)    ← Заработал $127
```

**Главное:**
- ✅ Есть "Загрузка данных" - бот работает
- ✅ Есть "LSTM прогноз" - нейросеть анализирует
- ✅ Есть "Открыл позицию" - бот торгует
- ✅ Нет "ERROR" - всё хорошо

---

## ⚙️ КОНФИГУРАЦИЯ

### В веб-интерфейсе:
Откройте http://localhost:5000/config

Там 6 готовых наборов параметров:
- **Консервативный** - мало рискует, мало прибыли
- **Сбалансированный** - золотая середина
- **Агрессивный** - много рискует, много прибыли

Просто выберите один! Все параметры можно менять в реальном времени.

### В .env файле:
```env
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
TESTNET=true              # Всегда true пока не уверены!
BYBIT_API_KEY=ваш_ключ    # Если есть ключи от Bybit
BYBIT_API_SECRET=ваш_секрет
```

---

## 🧪 ПЕРЕД MAINNET - ОБЯЗАТЕЛЬНО!

### Шаг 1️⃣ - Тестируйте на testnet
```bash
python main.py --pairs 20 --testnet
```
Всегда `--testnet`! Это бесплатно!

### Шаг 2️⃣ - Проверьте на истории
```bash
python backtest.py --days 30
```
Покажет какой был бы результат на прошлых 30 днях.

Должно быть что-то вроде:
```
Всего сделок: 120
Выигрыши: 85 (70%)
Прибыль: +$2,450 (ROI +12%)
Max Drawdown: -8.5%
```

Если хорошо - можно пробовать на реальных деньгах (МАЛЫМИ ОБЪЕМАМИ!)

---

## 🎯 ТОП-3 ОШИБКИ НОВИЧКОВ

### ❌ Ошибка 1: Забыли --testnet
```bash
python main.py --pairs 20  # НЕПРАВИЛЬНО - реальные деньги!
```

### ✅ Правильно:
```bash
python main.py --pairs 20 --testnet  # OK - тестовая сеть
```

---

### ❌ Ошибка 2: Слишком много пар
```bash
python main.py --pairs 100 --testnet  # Слишком сложно!
```

### ✅ Правильно:
```bash
python main.py --pairs 20 --testnet   # Начните отсюда
python main.py --pairs 50 --testnet   # Потом увеличьте
```

---

### ❌ Ошибка 3: Не читаете логи
Просто запустили бот и ушли...

### ✅ Правильно:
Смотрите http://localhost:5000/dashboard
Или в терминале `tail -f logs/trading_bot.log`

---

## 📱 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### Проблема: "Connection refused" на localhost:5000
```bash
# Проверьте что веб-сервер запущен:
python run.py
```

### Проблема: "No module named 'tensorflow'"
Это OK! Бот будет работать без LSTM (медленнее, но работает).

### Проблема: Пароль не подходит
Стандартный: `admin` / `admin123`

Если вы его изменили - отредактируйте `.env` файл.

### Проблема: "Insufficient balance"
На testnet нет денег? Попросите их на faucet Bybit.

### Проблема: "API Key invalid"
Проверьте что в `.env` файле правильные ключи:
```bash
cat .env
```

---

## 💡 ЛУЧШИЕ ПРАКТИКИ

### ✅ ВСЕГДА делайте
1. Используйте `--testnet` первые недели
2. Проверьте бэктест перед mainnet
3. Начните с 20 пар
4. Смотрите логи каждый день
5. Читайте что-то из документации

### ❌ НИКОГДА не делайте
1. Не пропускайте testnet
2. Не вкладывайте всё сразу
3. Не игнорируйте stop loss
4. Не забывайте про риск-менеджмент
5. Не думайте что бот волшебный

---

## 🔗 ССЫЛКИ НА ВСЮ ДОКУМЕНТАЦИЮ

| Файл | Для кого |
|------|----------|
| **GETTING_STARTED.md** | 👈 ВЫ ЗДЕСЬ - для новичков |
| **VERIFICATION_REPORT.md** | Подробный отчет проверки |
| **BOT_USAGE_GUIDE.md** | Полное руководство (300+ строк) |
| **QUICK_REFERENCE.md** | Шпаргалка со всеми командами |
| **README.md** | Как работают 3 стратегии |
| **DOCS.md** | Техническое описание |

---

## 🎬 ДАЛЬШЕ ЧТО?

### Если прибыль хорошая:
1. Увеличьте количество пар: 20 → 50 → 100
2. Переходите на mainnet (малыми объемами!)
3. Мониторьте каждый день

### Если результаты плохие:
1. Прочитайте BOT_USAGE_GUIDE.md
2. Попробуйте другой preset конфига
3. Увеличьте количество дней бэктеста
4. Посмотрите какие пары лучше торговать

### Если хотите улучшить:
1. Читайте код (он простой)
2. Менять параметры в конфиге
3. Переучивайте LSTM модель:
   ```bash
   python main.py --pairs 50 --retrain --testnet
   ```

---

## 🚀 В ОДНУ КОМАНДУ

```bash
# Linux/Mac:
bash start.sh

# Windows:
python quickstart.py

# И выбирайте что хотите
```

---

## 📞 ВОПРОСЫ?

Ответы почти наверняка в файлах:
- VERIFICATION_REPORT.md - "диагностика проблем"
- BOT_USAGE_GUIDE.md - "FAQ"
- QUICK_REFERENCE.md - "часто задаваемые вопросы"

---

## ✅ ГОТОВО!

Вы знаете всё что нужно. Просто запустите:

```bash
python quickstart.py
```

И следуйте меню.

**Удачи!** 💰📈

---

*Документация написана максимально просто. Если что-то не понятно - смотрите другие файлы.*
