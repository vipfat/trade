# 🤖 Гибридный ИИ-торговый бот для Bybit Futures

Продвинутый скальперский бот для торговли криптофьючерсами, использующий комбинацию трёх мощных стратегий:
- **LSTM машинное обучение** (60%)
- **Mean Reversion анализ** (25%)
- **Microstructure анализ** (15%)

## 📊 Основные характеристики

- **Таймфрейм**: 5 минутные свечи
- **Торговые пары**: 100 пар (6-105 по объему, пропускаем топ-5)
- **Тип стратегии**: Гибридная (LSTM + Mean Reversion + Microstructure)
- **Ожидаемый месячный профит**: 30-75%
- **Средний профит на сделку**: 0.3-0.8%

## 🚀 Быстрый старт

```bash
# 1. Установка
pip install -r requirements.txt

# 2. Конфигурация
cp .env.example .env
# Отредактируйте .env с вашими API ключами

# 3. Запуск на testnet
python main.py --pairs 100 --interval 300 --testnet

# 4. Запуск на реальном аккаунте
python main.py --pairs 100 --interval 300
```

## 📁 Структура проекта

```
trade/
├── src/
│   ├── api/bybit_client.py         # Клиент для Bybit API
│   ├── models/lstm_model.py        # LSTM модель для предсказания
│   ├── strategies/
│   │   ├── mean_reversion.py       # Mean Reversion стратегия
│   │   └── microstructure.py       # Анализ order book
│   ├── bot/hybrid_bot.py           # Главный класс бота
│   └── utils/logger.py             # Логирование
├── main.py                         # Главный скрипт
├── backtest.py                     # Бэктестирование
└── requirements.txt                # Зависимости
```

## ⚡ Стратегия торговли

### Алгоритм принятия решения (Weighted Voting)

```
Для каждой торговой пары каждые 5 минут:

1. LSTM анализ (60% вес)
   └─ Предсказывает направление цены на +/-
   
2. Mean Reversion (25% вес)
   └─ Ловит отклонения от Bollinger Bands
   
3. Microstructure (15% вес)
   └─ Анализирует дисбаланс ордеров в order book

Результат: СИГНАЛ (BUY/SELL) + УВЕРЕННОСТЬ (0-100%)

Вход в сделку если:
- Уверенность > 65%
- Спред < 0.05%
- < 20 сделок по паре в день
- < 5 открытых позиций одновременно
```

### Управление позициями

- **Take Profit**: +1% от входа
- **Stop Loss**: -2% от входа
- **Leverage**: 10x
- **Размер позиции**: 100 USDT базовая

## 📊 Математика прибыльности

```
На один контракт (100 USDT, 10x leverage = 1000 USDT):
- Средний профит на сделку: 5 USDT (0.5%)
- Ожидаемые сделки в день: 200-300 (на 100 парах)
- Дневной профит: 1000-1500 USDT (1-1.5%)

Месячный профит: 20-45% (консервативно)
```

## ⚙️ Конфигурация (.env)

```env
BYBIT_API_KEY=ваш_ключ
BYBIT_API_SECRET=ваш_секрет
BYBIT_TESTNET=False

LEVERAGE=10
POSITION_SIZE_USDT=100
MAX_POSITIONS=5
MAX_LOSS_PERCENT=2.0
CONFIDENCE_THRESHOLD=0.65
```

## 🔧 Использование

### Запуск бота

```bash
# На testnet (безопасно для тестирования)
python main.py --pairs 50 --interval 300 --testnet

# На реальном счёте (осторожно!)
python main.py --pairs 100 --interval 300

# Опции:
# --pairs N      : Количество торговых пар (по умолчанию 100)
# --interval N   : Интервал между итерациями в сек (по умолчанию 300)
# --testnet      : Использовать testnet
```

### Бэктестирование

```bash
python backtest.py --symbol BTCUSDT --days 30
```

## 🔐 Безопасность

⚠️ **ВАЖНО!!!**

1. Используйте **только на testnet** перед реальными деньгами
2. Начните с **маленького размером позиции** (10-50 USDT)
3. **Никогда** не коммитьте `.env` файл
4. Используйте **API ключи без прав на вывод**
5. Мониторьте бота **первые 24 часа**

## 📈 Ожидаемые результаты

| Метрика | Значение |
|---------|----------|
| Win Rate | 55-65% |
| Сделок в день | 200-300 |
| Дневной ROI | 0.5-2.5% |
| Месячный ROI | 30-75% |
| Max Drawdown | 8-15% |

## 📝 Примеры

```bash
# Консервативная торговля (качество > количество)
python main.py --pairs 50 --interval 600

# Агрессивная торговля (максимум сделок)
python main.py --pairs 100 --interval 180

# Бэктест на Ethereum
python backtest.py --symbol ETHUSDT --days 60
```

## 🆘 Проблемы

**Модель не обучена:**
- Бот автоматически обучает при запуске на исторических данных

**Низкая win rate:**
- Отрегулируйте `CONFIDENCE_THRESHOLD` (выше = лучше качество)

**Слишком много Stop Loss:**
- Увеличьте `MAX_LOSS_PERCENT` на 0.5-1%
- Или уменьшите `POSITION_SIZE_USDT`

**API ошибки:**
- Проверьте интернет подключение
- Убедитесь в правильности API ключей
- Подождите минуту и попробуйте снова

---

✅ **Готов к использованию** | ⚠️ **Торгуйте на свой риск** | 📊 **Постоянно оптимизируется**