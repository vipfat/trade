# ⚡ БЫСТРЫЙ СТАРТ (5 МИНУТ)

## 1️⃣ Подготовка

```bash
# Установить зависимости
pip install -r requirements.txt

# Скопировать конфигурацию
cp .env.example .env

# Отредактировать .env (вставить API ключи от Bybit)
nano .env
```

## 2️⃣ Проверить конфигурацию

```bash
# Запустить проверку
python setup.py

# Должна вывести:
# ✅ API Credentials найдены
# ✅ Подключение успешно!
# ✅ Найдено N пар с объемом > 100k USDT
# ✅ Модель успешно обучена!
```

## 3️⃣ Тестирование на TESTNET

```bash
# Запустить бота на testnet (безопасно!)
python main.py --pairs 20 --interval 300 --testnet

# Результат: Бот будет торговать на виртуальные деньги!
# Проверяйте логи: tail -f logs/trading_bot.log
```

## 4️⃣ Запуск на РЕАЛЬНОМ счете

```bash
# ТОЛЬКО ПОСЛЕ ТЕСТИРОВАНИЯ!
# Начните с маленьким размером позиции

python main.py --pairs 50 --interval 300
```

---

## 📊 Архитектура в одной картинке

```
КАЖДЫЕ 5 МИНУТ:

┌─────────────────┐
│  100+ торговых  │
│      пар        │
└────────┬────────┘
         │
    ┌────▼────┐
    │ Получить│
    │  данные │  • Свечи (OHLCV)
    │ Bybit   │  • Order book
    └────┬────┘  • Тикер
         │
    ┌────▼────────────────────┐
    │  АНАЛИЗ СИГНАЛОВ        │
    ├────────────────────────┤
    │ 1. LSTM (60%)     │BUY│
    │ 2. Mean Rev (25%) │ ? │
    │ 3. Micro (15%)    │   │
    └────┬───────────────────┘
         │
    ┌────▼──────────────────┐
    │  Confidence > 65%? ✅ │
    │  Спред < 0.05%?   ✅ │
    │  < 5 позиций?     ✅ │
    └────┬─────────────────┘
         │
    ┌────▼────────────────┐
    │   ВЫПОЛНИТЬ ОРДЕР   │
    │  Market order       │
    │  Size зависит от    │
    │  уверенности        │
    └────┬────────────────┘
         │
    ┌────▼─────────────────┐
    │  УПРАВЛЕНИЕ ПОЗИЦИЯХ │
    │  • TP: +1%           │
    │  • SL: -2%           │
    │  • Auto-close        │
    └──────────────────────┘
```

---

## 🎯 Ожидаемые результаты

| День 1 | День 2-3 | День 4-7 | Месяц |
|--------|----------|----------|-------|
| ~30-50 сделок | ~100 сделок | Стабильный паттерн | **30-75% ROI** |
| Знакомство с сигналами | Первый профит | Оптимизация | Накопление капитала |

---

## ⚙️ Важные параметры (.env)

```env
# ТОРГОВЛЯ
LEVERAGE=10                    # Плечо (1-20x)
POSITION_SIZE_USDT=100        # Базовый размер позиции
MAX_POSITIONS=5               # Макс позиций одновременно
MAX_LOSS_PERCENT=2.0          # Stop Loss (%)

# МОДЕЛЬ
CONFIDENCE_THRESHOLD=0.65     # Минимальная уверенность (0-1)
LOOKBACK_PERIOD=100           # Свечей для LSTM (100 = ~8 часов)

# РЕЖИМ
BYBIT_TESTNET=False           # True = тестовый счет
```

---

## 🚀 Команды запуска

### На testnet (рекомендуется сначала!)
```bash
python main.py --pairs 20 --interval 300 --testnet
```

### На реальном счете
```bash
python main.py --pairs 100 --interval 300
```

### Параметры:
- `--pairs N` : Количество пар (по умолчанию 100)
- `--interval N` : Интервал между итерациями в сек (по умолчанию 300)
- `--testnet` : Использовать testnet вместо mainnet

### Бэктестирование
```bash
python backtest.py --symbol BTCUSDT --days 30
```

---

## 📈 Мониторинг работы

```bash
# Смотреть логи в реальном времени
tail -f logs/trading_bot.log

# Все логи за сегодня
cat logs/trading_bot.log | grep "$(date +%Y-%m-%d)"

# Только сделки
grep "торговля выполнена" logs/trading_bot.log

# Только ошибки
grep "ERROR" logs/trading_bot.log
```

---

## 🔒 Чек-лист безопасности

- [ ] API ключи установлены в .env
- [ ] Используются ключи БЕЗ права на вывод
- [ ] Протестировано на testnet 1+ день
- [ ] Начальный размер позиции: 10-50 USDT
- [ ] Мониторинг первые 24 часа
- [ ] .env НЕ закоммичен в git
- [ ] Бот запущен с `--testnet` флагом перед реальной торговлей

---

## 🆘 Если что-то не работает

### "Модель не обучена"
```bash
# Бот обучит сам при запуске, но можно переобучить:
python main.py --pairs 10 --interval 300
# Подождите 2-3 минуты, пока обучится
```

### "Низкие сигналы"
```bash
# Уменьшите порог уверенности в .env:
CONFIDENCE_THRESHOLD=0.60  # Было 0.65
```

### "Слишком много Stop Loss"
```bash
# Увеличьте stop loss в .env:
MAX_LOSS_PERCENT=2.5  # Было 2.0
```

### "API ошибки"
1. Проверьте интернет: `ping google.com`
2. Проверьте ключи в .env
3. Проверьте на Bybit, что API активирован
4. Перезагрузите бота

---

## 📚 Документация

- **README.md** - Полная документация проекта
- **DOCS.md** - Подробные технические docs
- **logs/trading_bot.log** - Логи работы

---

## 💡 Советы по оптимизации

1. **Начните консервативно**
   - CONFIDENCE_THRESHOLD: 0.70 (выше = лучше качество)
   - POSITION_SIZE_USDT: 50 (меньше = менее рискованно)
   - MAX_POSITIONS: 3 (меньше = больше контроля)

2. **Монитора результаты**
   - Win rate должна быть > 50%
   - Средний профит на сделку > 0

3. **Постепенно увеличивайте**
   - После 10 дней прибыли → увеличьте позицию на 20%
   - Если убытки → уменьшьте параметры

4. **Переобучайте модель**
   - Каждые 4 часа автоматически
   - Или вручную: перезагрузите бота

---

## 🎓 Дальнейшее обучение

- Изучите логи для понимания сигналов
- Запустите бэктест на разных парах
- Экспериментируйте с параметрами
- Читайте статьи про LSTM, Mean Reversion, Order Book анализ

---

**Готово к работе! Начните сейчас:**

```bash
# Копируем конфиг
cp .env.example .env

# Вставляем API ключи в .env
nano .env

# Проверяем
python setup.py

# Тестируем
python main.py --pairs 20 --interval 300 --testnet

# Следим за логами
tail -f logs/trading_bot.log
```

**Удачи в торговле! 🚀**
