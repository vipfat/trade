# 🚀 Шпаргалка: Запуск и мониторинг бота

## Самое важное (2 минуты)

```bash
# Терминал 1: Веб-интерфейс
python run.py
# Откройте http://localhost:5000 (admin/admin123)

# Терминал 2: Бот на testnet
python main.py --pairs 20 --testnet

# Смотри Dashboard каждые 5 минут
# Ищи "Открыл позицию" в логах - это значит бот торгует!
```

---

## Команды запуска

### 🟢 Для новичков (НАЧНИ ОТСЮДА)
```bash
# Самый простой способ - интерактивный меню
python quickstart.py

# ИЛИ вручную:
python main.py --pairs 20 --testnet --verbose
```

### 🟡 Для опытных
```bash
# Conservative (низкий риск)
python main.py --pairs 50 --config conservative --testnet

# Balanced (рекомендуется)
python main.py --pairs 100 --config balanced --testnet

# Aggressive (высокий риск)
python main.py --pairs 200 --config aggressive --testnet
```

### 🔴 Реальная торговля (ОСТОРОЖНО!)
```bash
# ТОЛЬКО после 3+ дней testnet!
# ТОЛЬКО после успешного бэктеста!
# ТОЛЬКО с консервативной конфигурацией!

python main.py --pairs 50 --config conservative
```

---

## Как понять что бот работает

### ✅ Хорошие признаки
- Логи обновляются каждые 5 минут
- Видны строки "LSTM прогноз для ..."
- Видны строки "Открыл позицию"
- Видны строки "Закрыл позицию" с профитом

### ❌ Плохие признаки
- Нет логов последние 10 минут → перезагрузить
- ERROR в логах → смотри текст ошибки
- "Connection refused" → проверить API ключи

---

## Обучение модели

```bash
# Первый запуск - автоматическое обучение
python main.py --pairs 20 --testnet

# Переобучить с нуля
python main.py --pairs 30 --retrain --testnet

# Проверить что модель существует
ls -la lstm_model.h5
```

---

## Мониторинг

### В веб-интерфейсе
```
http://localhost:5000 → Dashboard
- Bot Status: работает ли бот
- Log Lines: сколько логов
- Trades: сколько сделок
- Recent Logs: последние 20 строк (автообновление)
```

### В терминале
```bash
# Видеть логи в реальном времени
tail -f logs/trading_bot.log

# Только сделки
grep "Открыл\|Закрыл" logs/trading_bot.log

# Только ошибки
grep ERROR logs/trading_bot.log

# Последние 10 строк
tail -10 logs/trading_bot.log
```

---

## Бэктестирование

```bash
# Быстрый тест на 30 дней
python backtest.py --days 30

# Детальный тест
python backtest.py --days 30 --pairs 20 --verbose

# Смотри результаты:
# Win Rate > 55% ✓
# Profit > 10% ✓  
# Max Drawdown < 15% ✓
```

---

## Параметры

```
--pairs NUM          количество пар (20-200)
--interval SECS      интервал обновления (300 = 5 мин)
--testnet            использовать testnet
--config NAME        conservative/balanced/aggressive
--retrain            переобучить модель
--epochs NUM         количество эпох (20-50)
--verbose            детальные логи
--no-trade           только анализ, без сделок
```

---

## Проблемы

| Проблема | Решение |
|----------|---------|
| Нет логов | Ctrl+C, проверить API ключи, перезагрузить |
| "Connection refused" | Проверить .env, добавить API ключи |
| "ModuleNotFoundError" | `pip install -r requirements.txt` |
| Бот совсем не торгует | Может быть confidence threshold выше. Ждать 1 час |
| Слишком много убыточных | Переключись на conservative конфигурацию |

---

## Быстрая помощь

```bash
# Все параметры по умолчанию (не рекомендуется)
python main.py

# Очень подробные логи
python main.py --pairs 10 --testnet --verbose

# Только анализ, без реальных сделок
python main.py --pairs 20 --testnet --no-trade

# Обучение новой модели
python main.py --pairs 50 --testnet --retrain --epochs 30

# Удалить логи и начать с нуля
rm logs/trading_bot.log
```

---

## Статистика текущей сессии

```bash
# Количество сделок
grep "Открыл позицию" logs/trading_bot.log | wc -l

# Прибыльных сделок
grep "Закрыл позицию" logs/trading_bot.log | grep "+" | wc -l

# Убыточных сделок  
grep "Закрыл позицию" logs/trading_bot.log | grep "-" | wc -l

# Общий профит (приблизительно)
grep "Закрыл позицию" logs/trading_bot.log | tail -5
```

---

## Правильный порядок действий

```
1️⃣ python run.py                          (веб-интерфейс)
2️⃣ python main.py --pairs 20 --testnet   (бот на testnet)
3️⃣ Смотри Dashboard 1-2 часа
4️⃣ python backtest.py --days 30           (протестировать)
5️⃣ python main.py --config balanced      (если хорошо, то на mainnet)
6️⃣ Мониторь КАЖДЫЙ ДЕНЬ!
```

**БОЛЬШЕ НЕ РИСКУЙ В НАЧАЛЕ! СНАЧАЛА TESTNET 3+ ДНЕЙ!**

---

## Полные гайды

Для подробной информации читай:
- `BOT_USAGE_GUIDE.md` - Полное руководство
- `README.md` - Описание алгоритмов
- `DOCS.md` - Техническая документация

---

💡 **Совет:** Используй `python quickstart.py` для интерактивного выбора!
