# 🔐 Решение: Логин и пароль не подходят

## ❌ Проблема

При попытке входа в веб-интерфейс (http://localhost:5000) выдаёт ошибку аутентификации.

## ✅ Решение

### Способ 1️⃣: Проверить учетные данные в файле `.env`

1. **Откройте файл `.env` в текстовом редакторе** (в корне проекта)

2. **Найдите строки:**
   ```env
   ADMIN_USERNAME=admin
   ADMIN_PASSWORD=admin123
   ```

3. **Это ваши учетные данные:**
   - **Логин:** admin
   - **Пароль:** admin123

4. **Если нужно изменить пароль**, отредактируйте строку `ADMIN_PASSWORD` и сохраните файл

### Способ 2️⃣: Сбросить пароль на дефолтный

Если вы забыли пароль:

**На Windows:**
```bash
python -c "
import os
from dotenv import load_dotenv

# Прочитать текущий .env
with open('.env', 'r') as f:
    lines = f.readlines()

# Заменить пароль
with open('.env', 'w') as f:
    for line in lines:
        if line.startswith('ADMIN_PASSWORD'):
            f.write('ADMIN_PASSWORD=admin123\n')
        else:
            f.write(line)

print('✅ Пароль сброшен на admin123')
"
```

**На Linux/Mac:**
```bash
sed -i 's/^ADMIN_PASSWORD=.*/ADMIN_PASSWORD=admin123/' .env
echo "✅ Пароль сброшен на admin123"
```

### Способ 3️⃣: Просто скопировать готовый `.env`

Если файл `.env` испорчен или его нет, просто скопируй готовый:

**На Windows:**
```bash
copy .env.example .env
```

**На Linux/Mac:**
```bash
cp .env.example .env
```

Затем отредактируй только нужные параметры (API ключи).

---

## 📋 Содержимое файла `.env`

Вот какой должен быть файл `.env`:

```env
# Bybit API Keys (заполни свои!)
BYBIT_API_KEY=
BYBIT_API_SECRET=
BYBIT_TESTNET=true

# Web Interface Settings (учетные данные для входа)
WEB_PORT=5000
ADMIN_USERNAME=admin          # ← Логин
ADMIN_PASSWORD=admin123       # ← Пароль
SECRET_KEY=dev-secret-key-change-in-production

# Trading Parameters
LEVERAGE=10
POSITION_SIZE_USDT=100
MAX_POSITIONS=5
MAX_LOSS_PERCENT=2.0

# Model Parameters
LOOKBACK_PERIOD=100
RETRAIN_INTERVAL_HOURS=4
CONFIDENCE_THRESHOLD=0.65

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/trading_bot.log
```

---

## 🔑 Как изменить пароль на свой

1. Откройте файл `.env` в текстовом редакторе
2. Найдите строку: `ADMIN_PASSWORD=admin123`
3. Измените на: `ADMIN_PASSWORD=твой_новый_пароль`
4. Сохраните файл
5. Перезагрузите веб-интерфейс

**Пример:**
```env
ADMIN_PASSWORD=MySecurePassword123!
```

---

## 🚀 После исправления

1. **Перезагрузи веб-интерфейс:**
   ```bash
   python run.py
   # Или на Windows: просто запусти run.bat
   ```

2. **Откройся в браузере:** http://localhost:5000

3. **Введи учетные данные:**
   - Логин из `ADMIN_USERNAME`
   - Пароль из `ADMIN_PASSWORD`

4. **Готово! 🎉**

---

## ❓ Частые ошибки

### ❌ "No such file or directory: '.env'"
→ Создай файл `.env` из `.env.example`:
```bash
cp .env.example .env  # Linux/Mac
copy .env.example .env  # Windows
```

### ❌ "Invalid credentials"
→ Проверь что ты вводишь точно то, что в файле `.env`
→ Без пробелов и лишних символов!

### ❌ "Connection refused"
→ Веб-интерфейс не запущен
→ Запусти: `python run.py`

### ❌ "Port 5000 already in use"
→ Порт занят другой программой
→ Измени `WEB_PORT=5001` в файле `.env` и перезагрузи

---

## 🆘 Если ничего не помогает

Просто удали файл `.env` и создай заново:

**На Windows:**
```bash
del .env
copy .env.example .env
```

**На Linux/Mac:**
```bash
rm .env
cp .env.example .env
```

Затем отредактируй только `ADMIN_PASSWORD` если хочешь другой пароль.

**Дефолтные учетные данные:**
- Логин: `admin`
- Пароль: `admin123`

---

## 💡 Совет

Вместо стандартного пароля `admin123`, используй что-то посложнее:
```env
ADMIN_PASSWORD=Trade2025Bot!Secure#Password
```

Это безопаснее, особенно если вы публикуете репо на GitHub! 🔐
