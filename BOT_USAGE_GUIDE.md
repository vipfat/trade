# 🤖 Полное руководство: Запуск и обучение торгового бота

## 📋 Содержание
1. [Быстрый старт (2 минуты)](#быстрый-старт)
2. [Как понять что бот работает](#как-понять-что-бот-работает)
3. [Обучение LSTM модели](#обучение-lstm-модели)
4. [Мониторинг в реальном времени](#мониторинг-в-реальном-времени)
5. [Бэктестирование](#бэктестирование)
6. [Команды и параметры](#команды-и-параметры)

---

## Быстрый старт

### Шаг 1: Откройте 2 терминала

**Терминал 1 - Веб-интерфейс:**
```bash
python run.py
```
Откройте http://localhost:5000 и введите: **admin / admin123**

**Терминал 2 - Торговый бот:**
```bash
python main.py --pairs 20 --interval 300 --testnet
```

### Шаг 2: Смотрите логи на Dashboard

Перейдите на **Dashboard** в веб-интерфейсе и смотрите логи в реальном времени.

---

## Как понять что бот работает

### 🟢 Признаки что бот работает нормально:

1. **Логи обновляются каждые 5 минут** на странице Dashboard → Logs
   ```
   2025-12-19 16:00:00,123 - trading_bot - INFO - === Итерация #1 ===
   2025-12-19 16:00:01,234 - trading_bot - INFO - Получено 100 пар для торговли
   2025-12-19 16:00:05,456 - trading_bot - INFO - LSTM прогноз для BTC/USDT: LONG (+78%)
   ```

2. **Видны "LSTM прогноз" строки** - значит модель анализирует данные:
   ```
   LSTM прогноз для ETH/USDT: SHORT (-65%)
   Mean Reversion сигнал для SOL/USDT: NEUTRAL (не торгуем)
   Microstructure анализ для ADA/USDT: LONG (+42%)
   ```

3. **Появляются "Открыл позицию" строки** - бот совершает сделки:
   ```
   ✅ Открыл позицию BTC/USDT: вход 43250 USDT, TP 43683 (+1%), SL 42425 (-2%)
   ```

4. **Видны "Закрыл позицию" строки** - сделки закрываются:
   ```
   💰 Закрыл позицию ETH/USDT: прибыль +45 USDT (+0.45%)
   ```

### 🟠 Признаки проблем:

| Признак | Проблема | Решение |
|---------|----------|--------|
| **Нет логов последние 10 минут** | Бот зависло или упал | Перезагрузи бот (Ctrl+C, потом снова run) |
| **ERROR в логах** | Ошибка API или модели | Смотри строку с ERROR и исправляй по описанию |
| **"Connection refused"** | Нет доступа к Bybit API | Проверь API ключи в Environment странице |
| **"ModuleNotFoundError"** | Не установлена библиотека | Запусти `pip install -r requirements.txt` |

---

## Обучение LSTM модели

### Вариант 1️⃣: Автоматическое обучение (РЕКОМЕНДУЕТСЯ)

При первом запуске бот автоматически обучает модель:

```bash
python main.py --pairs 100 --interval 300 --testnet
```

**Что происходит:**
```
🤖 === Инициализация бота ===
📊 Получаю исторические данные для обучения...
📈 Получаю данные для BTC/USDT... ✓
📈 Получаю данные для ETH/USDT... ✓
...
🧠 Обучаю модель на 2500 свечах...
   Epoch 1/20: loss=0.0234
   Epoch 2/20: loss=0.0198
   ...
   Epoch 20/20: loss=0.0089
✅ Модель успешно обучена!
```

### Вариант 2️⃣: Переобучение на свежих данных

Если хочешь обновить модель на новые данные:

```bash
python main.py --pairs 50 --retrain --testnet
```

**Параметры переобучения:**
- `--retrain` - переобучить модель с нуля
- `--epochs 50` - количество эпох (по умолчанию 20)
- `--lookback 200` - сколько свечей использовать (по умолчанию 100)

### Вариант 3️⃣: Используй готовый скрипт обучения

```bash
python examples.py --train-lstm --pairs 20 --lookback 100
```

**Что это делает:**
- Получает данные для 20 пар
- Обучает LSTM модель на 100 свечах (примерно 8-9 часов торговли)
- Сохраняет модель в файл `lstm_model.h5`

### 📊 Как проверить что модель обучилась?

**В веб-интерфейсе:**
1. Откройте страницу **Logs**
2. Поищите строку: `Модель успешно обучена!`
3. Смотрите значения loss (должны уменьшаться):
   ```
   Epoch 1/20: loss=0.0234
   Epoch 20/20: loss=0.0089  ← loss должен уменьшаться
   ```

**В терминале:**
```bash
# Проверить что файл модели существует
ls -la lstm_model.h5
# Если файл есть - модель сохранена ✓
```

---

## Мониторинг в реальном времени

### 📊 Dashboard (рекомендуется)

Откройте http://localhost:5000 и нажмите **Dashboard**

Вы увидите:
- **Bot Status** - статус работы ("Running", "Idle", etc)
- **Log Lines** - количество строк в логе
- **Trades** - количество совершенных сделок
- **Errors** - количество ошибок
- **Recent Logs** - последние 20 логов (обновляются каждые 5 сек)
- **Recent Trades** - последние сделки с профитом

### 📝 Логи (детально)

**На странице Logs:**
1. **Auto-refresh** - включи автообновление
2. **Search** - поиск по ключевым словам
3. **Filter** - фильтрация по типу (ERROR, WARNING, INFO, TRADE)

**Полезные фильтры:**
```
Ищи "LSTM прогноз" → видны все прогнозы модели
Ищи "Открыл позицию" → все входы в сделки
Ищи "Закрыл позицию" → все выходы и прибыли
Ищи "ERROR" → все ошибки (если есть)
```

### 💻 Терминал (прямой вывод)

```bash
# Смотреть логи в реальном времени
tail -f logs/trading_bot.log

# Только ошибки
grep ERROR logs/trading_bot.log

# Только сделки
grep "Открыл позицию\|Закрыл позицию" logs/trading_bot.log

# Последние 50 строк
tail -50 logs/trading_bot.log
```

---

## Бэктестирование

### 🧪 Тестирование стратегии на исторических данных

```bash
# Базовый бэктест на 30 дней
python backtest.py --days 30

# Детальный бэктест с 10 парами
python backtest.py --days 30 --pairs 10 --verbose

# Бэктест с BALANCED конфигурацией
python backtest.py --days 30 --config balanced
```

**Результат:**
```
═══════════════════════════════════════════
         📊 РЕЗУЛЬТАТЫ БЭКТЕСТА
═══════════════════════════════════════════
Период:        2025-09-19 to 2025-10-19
Сделок:        256 (win: 159, loss: 97)
Win Rate:      62.1%
Общий профит:  +$1,245 (+12.4%)
Max Drawdown:  -8.7%
Sharpe Ratio:  1.84
═══════════════════════════════════════════
```

### 📈 Интерпретация результатов

| Метрика | Хорошо | Норм | Плохо |
|---------|--------|------|-------|
| **Win Rate** | >60% | 50-60% | <50% |
| **Profit** | >20% в месяц | 5-20% | <5% |
| **Drawdown** | <10% | 10-20% | >20% |
| **Sharpe Ratio** | >2.0 | 1.0-2.0 | <1.0 |

---

## Команды и параметры

### 🚀 Основные команды

```bash
# TESTNET (РЕКОМЕНДУЕТСЯ для начала)
python main.py --pairs 20 --interval 300 --testnet

# MAINNET (реальные деньги - используй с осторожностью!)
python main.py --pairs 100 --interval 300

# С определенной конфигурацией
python main.py --pairs 50 --config conservative --testnet
python main.py --pairs 100 --config balanced --testnet
python main.py --pairs 200 --config aggressive --testnet
```

### ⚙️ Параметры запуска

```bash
python main.py [параметры]

Основные параметры:
  --pairs NUM           Количество торговых пар (по умолчанию 100)
  --interval SECONDS    Интервал обновления в секундах (по умолчанию 300 = 5 мин)
  --testnet             Использовать testnet вместо mainnet (РЕКОМЕНДУЕТСЯ!)
  --config NAME         Использовать конфигурацию (conservative/balanced/aggressive)
  
Параметры обучения:
  --retrain             Переобучить модель с нуля
  --epochs NUM          Количество эпох обучения (по умолчанию 20)
  --lookback NUM        Количество свечей для обучения (по умолчанию 100)
  
Параметры отладки:
  --verbose             Подробные логи
  --no-trade            Только анализ, без совершения сделок
```

### 📊 Примеры запуска

```bash
# 1️⃣ Первый раз: обучение + тестирование
python main.py --pairs 20 --interval 300 --testnet --retrain --verbose

# 2️⃣ Консервативная торговля на testnet
python main.py --pairs 50 --config conservative --testnet

# 3️⃣ Сбалансированная торговля (рекомендуется)
python main.py --pairs 100 --config balanced --testnet

# 4️⃣ Агрессивная торговля (высокий риск)
python main.py --pairs 200 --config aggressive --testnet

# 5️⃣ Только анализ без сделок (для отладки)
python main.py --pairs 30 --testnet --no-trade --verbose

# 6️⃣ На mainnet (РЕАЛЬНЫЕ ДЕНЬГИ!)
python main.py --pairs 100 --config conservative
```

---

## 🎯 Рекомендуемый план работы

### День 1-2: Тестирование на testnet

```bash
# Терминал 1: Веб-интерфейс
python run.py

# Терминал 2: Бот на testnet с консервативной стратегией
python main.py --pairs 20 --interval 300 --testnet --verbose

# Смотри Dashboard каждые 5 минут
# Проверяй логи в реальном времени
# Убедись что бот совершает сделки и закрывает их
```

### День 3-5: Оптимизация параметров

```bash
# Попробуй разные конфигурации
python main.py --pairs 50 --config balanced --testnet

# Смотри статистику на Dashboard
# Сравни профиты между конфигурациями
# Выбери лучшую
```

### День 6-7: Бэктестирование

```bash
# Протестируй стратегию на истории
python backtest.py --days 30 --config balanced

# Убедись что win rate > 55%
# Max drawdown < 15%
# Sharpe ratio > 1.5
```

### День 8+: Реальная торговля (опционально)

```bash
# ТОЛЬКО если бэктест показал хорошие результаты!
# И ТОЛЬКО с консервативной конфигурацией!
# И ТОЛЬКО с небольшой суммой денег для начала!

python main.py --pairs 50 --config conservative

# Мониторь КАЖДЫЙ ДЕНЬ в течение недели
# Если профит положительный и нет больших потерь - увеличь размер
```

---

## ❓ Частые вопросы

### Q: Сколько времени бот обучается?
**A:** 3-5 минут на 500 свечах. При первом запуске это произойдет автоматически.

### Q: Как часто нужно переобучать модель?
**A:** Каждые 7 дней (или каждые 100+ сделок) для лучшей точности.

### Q: Бот может потерять деньги?
**A:** ДА! Даже с хорошей стратегией есть риск. **ВСЕГДА используй testnet перед mainnet!**

### Q: Как узнать текущий профит?
**A:** На Dashboard смотри логи с "Закрыл позицию" - там показана прибыль на каждую сделку.

### Q: Что делать если бот упал?
**A:** 
1. Смотри логи (page Logs → filter ERROR)
2. Проверь API ключи (Environment страница)
3. Перезагрузи бот (Ctrl+C и снова `python main.py...`)

### Q: Можно ли менять параметры во время работы?
**A:** Не рекомендуется. Закрой позиции, останови бот (Ctrl+C), измени параметры, запусти заново.

---

## 🆘 Быстрая помощь

```bash
# Проверить что бот работает
tail -f logs/trading_bot.log

# Найти последнюю сделку
grep "Закрыл позицию" logs/trading_bot.log | tail -1

# Посчитать количество сделок
grep "Открыл позицию" logs/trading_bot.log | wc -l

# Посчитать прибыльные сделки
grep "Закрыл позицию" logs/trading_bot.log | grep "+" | wc -l

# Посчитать убыточные сделки
grep "Закрыл позицию" logs/trading_bot.log | grep "-" | wc -l

# Удалить старые логи и начать с нуля
rm logs/trading_bot.log
```

---

## 📞 Получить помощь

Если что-то не понятно:
1. Смотри **Documentation** → Полный README
2. Проверь **Logs** → ищи ERROR сообщения
3. Используй `--verbose` флаг для детальных логов
4. Читай комментарии в коде `src/bot/hybrid_bot.py`

**Удачи в торговле!** 💰📈
