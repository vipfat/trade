# 🌐 Trading Bot Web Interface

Полнофункциональный веб-интерфейс для управления AI-торговым ботом Bybit с поддержкой редактирования параметров, мониторинга логов и восстановления конфигурации.

## 🎯 Возможности

### 📊 Dashboard
- Мониторинг статуса бота в реальном времени
- Просмотр статистики логов (количество строк, ошибок, сделок)
- Отображение текущих параметров бота
- Последние логи и сделки

### ⚙️ Конфигурация
- **Редактирование всех параметров:**
  - Параметры бота (пары, интервал, плечо, размер позиции)
  - Веса стратегий (LSTM, Mean Reversion, Microstructure) с валидацией
  - Управление рисками (TP, SL, лимиты, drawdown)
  - Параметры торговли (таймфрейм, спред, проскальзывание)
  - Логирование

- **Готовые конфигурации (6 штук):**
  - CONSERVATIVE - стабильность и низкий риск
  - BALANCED - оптимальный баланс (рекомендуется)
  - AGGRESSIVE - максимум прибыли при высоком риске
  - ML_FOCUSED - уклон на LSTM модель
  - MR_FOCUSED - уклон на Mean Reversion
  - MS_FOCUSED - уклон на Microstructure

- **Управление резервными копиями:**
  - Автоматическое создание резервных копий перед изменением
  - Просмотр всех созданных копий
  - Восстановление из любой резервной копии
  - Восстановление параметров по умолчанию (Defaults)

### 📝 Логи
- **Просмотр логов в реальном времени:**
  - Автоматическое обновление каждые 5 секунд
  - Прокрутка до конца при добавлении новых строк
  - Последние 500 строк логов

- **Поиск и фильтрация:**
  - Поиск по ключевым словам
  - Фильтрация по уровню (ERROR, WARNING, INFO, DEBUG, TRADE)
  - Вкладки для сделок и ошибок

- **Управление логами:**
  - Скачивание логов в файл
  - Очистка логов
  - Автообновление (на/выкл)
  - Статистика (всего строк, сделок, ошибок, размер)

### 🔐 Environment Variables
- **Управление .env файлом:**
  - Редактирование API ключей Bybit
  - Управление учетными данными администратора
  - Параметры веб-интерфейса (port, host)
  - Добавление дополнительных переменных

- **Безопасность:**
  - Маскирование чувствительных данных (ключи, пароли)
  - Не сохраняются замаскированные значения при повторном редактировании
  - Предупреждение о необходимости не коммитить .env

## 🚀 Установка и запуск

### Шаг 1: Установить зависимости

```bash
# В директории проекта
pip install -r web/requirements.txt
```

### Шаг 2: Запустить веб-интерфейс

```bash
# Базовый запуск (localhost:5000)
python web_runner.py

# На custom port
python web_runner.py --port 8080

# С debug режимом (auto-reload при изменении файлов)
python web_runner.py --debug

# Доступно со всех сетевых интерфейсов
python web_runner.py --host 0.0.0.0 --port 5000
```

### Шаг 3: Открыть в браузере

```
http://localhost:5000
```

## 🔑 Аутентификация

### Учетные данные по умолчанию:
- **Username:** `admin`
- **Password:** `admin123`

### Изменение учетных данных:

1. Перейдите на страницу Environment Variables (`/env-config`)
2. Измените `ADMIN_USERNAME` и `ADMIN_PASSWORD`
3. Сохраните изменения

## 📋 Структура проекта

```
web/
├── app.py                      # Flask приложение с API endpoints
├── web_runner.py               # Запуск веб-интерфейса
├── requirements.txt            # Зависимости
├── templates/
│   ├── base.html              # Базовый шаблон
│   ├── index.html             # Главная страница
│   ├── dashboard.html         # Dashboard
│   ├── config.html            # Конфигурация
│   ├── logs.html              # Просмотр логов
│   └── env.html               # Environment variables
└── static/
    └── js/
        └── main.js            # JavaScript функции
```

## 🔌 API Endpoints

### Аутентификация
- `POST /api/login` - Вход в систему
- `POST /api/logout` - Выход
- `GET /api/is-authenticated` - Проверка аутентификации

### Конфигурация
- `GET /api/config` - Получить текущую конфигурацию
- `POST /api/config` - Обновить конфигурацию
- `POST /api/config/defaults` - Восстановить defaults
- `GET /api/config/backups` - Список резервных копий
- `POST /api/config/restore/<backup_name>` - Восстановить из копии

### Логи
- `GET /api/logs` - Получить последние логи
- `GET /api/logs/stats` - Статистика логов
- `POST /api/logs/search` - Поиск по логам
- `GET /api/logs/trades` - Последние сделки
- `POST /api/logs/clear` - Очистить логи
- `GET /api/logs/download` - Скачать логи

### Environment
- `GET /api/env` - Получить переменные окружения
- `POST /api/env` - Обновить переменные

### Информация о системе
- `GET /api/status` - Статус бота
- `GET /api/system-info` - Информация о системе

## 💡 Советы по использованию

### Первый запуск
1. Установите зависимости: `pip install -r web/requirements.txt`
2. Запустите интерфейс: `python web_runner.py`
3. Перейдите на `http://localhost:5000`
4. Войдите (admin/admin123)
5. Перейдите на Environment Variables и добавьте API ключи Bybit
6. Перейдите на Конфигурацию и выберите готовый preset

### Редактирование конфигурации
- Используйте готовые presets как основу
- Валидация весов стратегий автоматическая
- Все изменения сохраняются в резервные копии
- Можно восстановить любую предыдущую версию

### Мониторинг логов
- Включите автообновление на вкладке Логов
- Используйте фильтры для поиска конкретных событий
- Проверяйте сделки на вкладке "Сделки"
- Анализируйте ошибки на вкладке "Ошибки"

## 🔧 Развертывание

### На Linux сервере

```bash
# Установить зависимости
pip install -r web/requirements.txt

# Запустить фоном с nohup
nohup python web_runner.py --host 0.0.0.0 --port 8080 > web.log 2>&1 &

# Или с systemd (создать /etc/systemd/system/trading-bot-web.service)
[Unit]
Description=Trading Bot Web Interface
After=network.target

[Service]
Type=simple
User=trading
WorkingDirectory=/home/trading/trade
ExecStart=/usr/bin/python3 web_runner.py --host 0.0.0.0 --port 8080
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### С использованием Docker

```dockerfile
FROM python:3.10

WORKDIR /app
COPY web/requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "web_runner.py", "--host", "0.0.0.0", "--port", "5000"]
```

## ⚠️ Безопасность

### Рекомендации:
1. **Никогда** не коммитьте .env файл в git
2. Используйте **сильные пароли** для администратора
3. Закройте доступ к веб-интерфейсу **firewall** или VPN
4. Измените дефолтные учетные данные
5. Используйте HTTPS при доступе через интернет
6. Регулярно проверяйте логи на подозрительную активность

### Переменные окружения для безопасности:

```bash
# .env
FLASK_ENV=production        # Отключить debug в production
SECRET_KEY=your_secret_key  # Установить secret key
ADMIN_USERNAME=your_username
ADMIN_PASSWORD=your_password
```

## 🐛 Решение проблем

### Порт уже занят
```bash
# Используйте другой port
python web_runner.py --port 8080
```

### Ошибка "Template not found"
```bash
# Убедитесь что находитесь в директории проекта
cd /workspaces/trade
python web_runner.py
```

### Ошибка аутентификации
```bash
# Проверьте переменные в .env
grep ADMIN /workspaces/trade/.env

# Если неправильно, отредактируйте .env напрямую
nano /workspaces/trade/.env
```

## 📞 Быстрые команды

```bash
# Запуск с default параметрами
python web_runner.py

# Запуск с debug (auto-reload)
python web_runner.py --debug

# Запуск на port 8080
python web_runner.py --port 8080

# Запуск доступно со всех IP (для VPS/облака)
python web_runner.py --host 0.0.0.0 --port 8080

# Запуск фоном на Linux
nohup python web_runner.py > web.log 2>&1 &

# Проверка логов
tail -f web.log
```

## 📊 Интеграция с ботом

Веб-интерфейс **НЕ запускает бота автоматически** - это утилита управления.

Для запуска бота используйте отдельно:
```bash
python main.py --pairs 100 --interval 300 --testnet
```

Веб-интерфейс будет:
- ✓ Показывать логи бота в реальном времени
- ✓ Позволять редактировать конфигурацию
- ✓ Сохранять резервные копии параметров
- ✓ Отслеживать статистику и сделки

## 🎓 Примеры использования

### Сценарий 1: Первое включение на testnet

1. Откройте веб-интерфейс: `http://localhost:5000`
2. Войдите (admin/admin123)
3. Перейдите на Environment и добавьте Bybit API ключи
4. На Конфигурации выберите preset CONSERVATIVE
5. Запустите бота: `python main.py --pairs 20 --interval 300 --testnet`
6. Наблюдайте логи на Dashboard
7. После 1-2 дней тестирования проверьте результаты и переходите на mainnet

### Сценарий 2: Оптимизация параметров

1. Запустите бота с BALANCED конфигурацией
2. Мониторьте статистику на Dashboard
3. На Конфигурации отрегулируйте веса стратегий
4. Сохраните конфигурацию (автоматически создается резервная копия)
5. Наблюдайте влияние на логи и сделки
6. При необходимости восстановите предыдущую конфигурацию

### Сценарий 3: Переключение между режимами

1. Откройте Конфигурацию
2. Выберите необходимый preset (AGGRESSIVE для максимума, CONSERVATIVE для стабильности)
3. Сохраните
4. Перезагрузите бота: `python main.py --pairs 100 --interval 300`

## 🚀 Версия

**Версия:** 1.0.0  
**Последнее обновление:** Декабрь 2025  
**Статус:** Production Ready

## 📄 Лицензия

MIT License - свободно используйте в своих проектах.

---

**Удачи в торговле! 💰📈**
